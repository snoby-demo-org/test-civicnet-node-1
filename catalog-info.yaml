---
# This file registers this component in Backstage. The whole file is a templated
# asset — fields are filled in when this repo is created from the CivicNet Node
# template.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: test-civicnet-node-1
  description: CivicNet node scaffolded from Backstage template — fully documented showcase
  annotations:
    github.com/project-slug: snoby/test-civicnet-node-1
    backstage.io/techdocs-ref: dir:.
    # Optional, but makes the entity's ownership + lifecycle richer in the UI:
    # backstage.io/techdocs-entity: test-civicnet-node-1
    # github.com/team-slug: my-org/my-team
spec:
  type: service
  lifecycle: experimental
  owner: guest
  system: civicnet-system
  # Links the node to its JSON-RPC API in the catalog graph and APIs tab.
  providesApis:
    - civicnet-node-rpc
  # Models the node's on-disk data dependency.
  dependsOn:
    - resource:default/civicnet-node-data
